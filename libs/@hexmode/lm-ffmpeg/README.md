# lm-ffmpeg

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build lm-ffmpeg` to build the library.

## Running unit tests

Run `nx test lm-ffmpeg` to execute the unit tests via [Vitest](https://vitest.dev/).
