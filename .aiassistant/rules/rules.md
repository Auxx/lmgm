---
apply: always
---

- Use strict type checking
- Prefer type inference when the type is evident
- Do NOT use the `any` type; use `unknown` when type is uncertain
- Always use arrow function expressions to define class methods
- Always mark arrow function expressions as `readonly`
